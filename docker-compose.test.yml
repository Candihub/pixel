version: '2.1'

services:
  db:
    image: postgres
    env_file: env.d/test
  web:
    build:
      context: .
      args:
        IS_NOT_PRODUCTION: "true"
    env_file: env.d/test
    depends_on:
      - db
